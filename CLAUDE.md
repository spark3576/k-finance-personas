# 작업 지침

본 저장소에서 작업할 때 지켜야 할 사항입니다. 세션을 시작하면 아래 순서를 따릅니다.

## 1. 세션 시작 절차

1. `docs/00_현황판.md`를 읽고 현재 상태와 다음 작업을 확인합니다.
2. `docs/04_문체기준.md`를 읽고 서술 기준을 확인합니다.
3. `docs/03_결정기록.md`를 읽고 이미 확정된 결정을 확인합니다. 같은 논의를 반복하지 않습니다.
4. 상태를 점검합니다.

```bash
git log --oneline -5 && git status -sb && git tag | tail -3
python3 docs/tools/check_style.py
python3 docs/tools/sync_shared.py --check
```

## 2. 문서를 고칠 때

- 서술 기준을 먼저 확인하고, 고친 뒤 `python3 docs/tools/check_style.py`로 점검합니다.
- 규칙 식별자 R1부터 R10까지와 오류 유형 식별자 AP1부터 AP17까지는 재번호하지 않습니다. 과거 산출물이 이를 참조합니다.
- 법령 조항 번호와 정량 임계치는 원문 그대로 옮깁니다.
- 개정 이력과 작업 경위는 본문에 넣지 않고 `docs/06_개정이력.md`에 기록합니다.

## 3. 공용 자산을 고칠 때

정본은 최상위 `shared/` 입니다. 페르소나 폴더의 `shared/` 는 사본이므로 직접 고치지 않습니다.

```bash
# 정본을 고친 뒤 사본을 갱신합니다
python3 docs/tools/sync_shared.py
python3 docs/tools/sync_shared.py --check
```

## 4. 작업을 마칠 때

1. `docs/00_현황판.md`의 제1절과 제4절을 갱신합니다.
2. 새로 확정한 결정이 있으면 `docs/03_결정기록.md`에 추가합니다.
3. 시험을 수행했으면 `docs/02_시험기록.md`에 추가합니다.
4. 판번호를 올렸으면 `docs/06_개정이력.md`에 변경 내역을 기록합니다.
5. `HANDOFF.md`를 갱신합니다.
6. 아래를 실행하여 전부 통과하는지 확인한 뒤 커밋합니다.

```bash
python3 docs/tools/check_style.py
python3 docs/tools/sync_shared.py --check
```

## 5. 커밋 규칙

- 제목은 「판번호 — 변경 요지」 형식으로 적습니다.
- 본문에는 무엇을 왜 바꾸었는지 적습니다.
- 기능 변경이 없는 정비 작업은 판번호의 마지막 자리만 올립니다.
- 태그는 판번호가 올라갈 때만 생성합니다.

## 6. 배포

```bash
python3 docs/tools/build_packages.py
```

압축 파일은 형상 관리 대상이 아닙니다. claude.ai 개인 스킬 등록은 사용자가 직접 수행합니다.

## 7. 주의

- 저장소 밖의 사용자 파일을 임의로 고치지 않습니다.
- 확인하지 못한 사실을 추정으로 채우지 않습니다. 「미기록」으로 남기고 그 사실을 밝힙니다.
- 실재하는 회사명과 인명을 예시에 쓰지 않습니다.
